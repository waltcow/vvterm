import SwiftUI

enum StatsSheetClosePlacement {
    case automatic
    case leading
    case trailing
}

enum StatsPresentationSize {
    static let standard = CGSize(width: 720, height: 640)
    static let large = CGSize(width: 900, height: 760)
}
